name: Build and Deploy to Azure

on:
  push:
    branches: [ main ]
  workflow_dispatch:
  pull_request:
    branches: [ '*' ]
env:
  IMAGE_NAME: zavastore

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Build and push container to ACR
        run: |
          az acr build \
            --registry ${{ vars.AZURE_CONTAINER_REGISTRY_NAME }} \
            --image ${{ env.IMAGE_NAME }}:${{ github.sha }} \
            --image ${{ env.IMAGE_NAME }}:latest \
            --file ./src/Dockerfile \
            ./src

      - name: Deploy to App Service
        run: |
          az webapp config container set \
            --name ${{ vars.AZURE_APP_SERVICE_NAME }} \
            --resource-group ${{ vars.AZURE_RESOURCE_GROUP }} \
            --docker-custom-image-name ${{ vars.AZURE_CONTAINER_REGISTRY_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.sha }}
          
          az webapp restart \
            --name ${{ vars.AZURE_APP_SERVICE_NAME }} \
            --resource-group ${{ vars.AZURE_RESOURCE_GROUP }}

      - name: Logout from Azure
        if: always()
        run: az logout
